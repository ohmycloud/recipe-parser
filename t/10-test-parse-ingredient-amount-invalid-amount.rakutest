plan 3;

use Test;
use Recipe::Grammar;

my @cases = ("(1.5", "1/2)", "(7");

for @cases -> $input {
    my $m = Recipe::Grammar.parse($input, rule => 'ingredient-amount');
    nok $m, "ingredient-amount('$input')";
}

done-testing;
