plan 2;

use Test;
use Recipe::Grammar;

my @cases = ["/* unclosed", "unclosed */"];

for @cases -> $input {
    my $m = Recipe::Grammar.parse($input, rule => 'comment');
    nok $m, "comment('$input')";
}

done-testing;
